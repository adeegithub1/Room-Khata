// src/views/SettingsView.jsx
// TODO: Migrate settings panel (profile, language toggle, logout) from original app
export default function SettingsView() {
  return (
    <div className="flex flex-col items-center justify-center h-full">
      <p className="text-2xl font-black" style={{ color: "var(--indigo)" }}>
        Settings — Coming in Step 2
      </p>
    </div>
  );
}
